import { SVGBackground } from "../../components/SVG-background";
import { Menu } from "../../components/menu";

export const Main = () => {
  return (
    <div>
      <SVGBackground />
      <Menu />
    </div>
  );
};
